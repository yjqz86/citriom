$(document).ready(function(){

  // Formato de los Select
  $("select").select2({
    placeholder: "Seleccione...",
    allowClear: true,
    debug: true,
    dropdownCssClass: 'dropdown-inverse'
  });

});
